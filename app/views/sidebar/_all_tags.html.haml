.section-header
  = link_to sidebar_overview_path, :class => 'iosstyle fl ios-back', :remote => true do
    %span
    Back
  Find a Tag
%section.pane.tag-nav
  - ("A".."Z").each do |letter|
    = link_to letter, tags_by_first_letter_path(letter), :class => 'letter-link', :remote => true
  =# link_to "Other", tags_by_first_letter_path("!"), :class => 'letter-link other', :remote => true
.section.tag-results